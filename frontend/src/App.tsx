function App() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center gap-6 bg-[var(--color-primary)] text-white">
      <h1 className="text-body-1 font-semibold">🚀 Tailwind v4 + @theme 작동 중!</h1>
      <button className="bg-white text-[var(--color-primary)] px-4 py-2 rounded font-semibold">
        버튼
      </button>
    </main>
  );
}

export default App;
